{
  "name": "example1",
  "version": "1.0.0",
  "devDependencies": {
    "@nfts/gmf": "workspace:*"
  },
  "scripts": {
    "test": "gmf build --test",
    "dev": "gmf build --watch",
    "build": "gmf build"
  }
}
