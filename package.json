{
  "name": "initializer",
  "version": "0.0.0",
  "description": "bootstrap personal project",
  "repository": "https://github.com/leiwenpeng0424/initializer.git",
  "author": "雷文鹏 <leiwenpeng1994@outlook.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "toolkit dev",
    "build": "toolkit build",
    "test": "toolkit runTests",
    "release": "toolkit release",
    "preview": "toolkit preview",
    "build-helper": "toolkit build --scope=env",
    "onestop-push": "git add -A && git commit -m 'auto commit' && git push"
  },
  "engines": {
    "node": ">=10",
    "pnpm": ">=6"
  },
  "dependencies": {
    "prettier": "^2.2.1",
    "@initializer/buildhelper": "workspace:*"
  }
}
