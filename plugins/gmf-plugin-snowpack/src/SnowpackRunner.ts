import { Configuration } from '@nfts/gmf';
import {
	loadConfiguration,
	SnowpackConfig,
	SnowpackUserConfig,
	startServer,
	createConfiguration
} from 'snowpack';

export class SnowpackRunner {
	public runAsync(): Promise<void> {
		// void
	}

	private _devServer() {}
}
