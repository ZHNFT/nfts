{
  "name": "{{project.name}}",
  "version": "0.0.0",
  "author": {
    "email": "{{user.email}}",
    "name": "{{user.name}}",
    "url": "{{user.url}}"
  },
  "description": "Description for your project",
  "eslintConfig": {
    "extends": [
      "@nfts"
    ]
  },
  "eslintIgnore": [
    "*.log*",
    "node_modules",
    "*.tsbuildinfo",
    ".npm",
    ".eslintcache",
    ".env",
    ".env.test",
    "dist",
    ".yarn/**",
    ".npn.*",
    "test"
  ]
}
