{
  "name": "{{project.name}}",
  "version": "0.0.0",
  "type": "module",
  "author": {
    "email": "{{user.email}}",
    "name": "{{user.name}}",
    "url": "{{user.url}}"
  },
  "description": "Description of the project",
  "eslintConfig": {
    "extends": [
      "@nfts"
    ]
  },
  "eslintIgnore": [
    "*.log*",
    "node_modules",
    "*.tsbuildinfo",
    ".npm",
    ".eslintcache",
    ".env",
    ".env.test",
    "dist",
    ".yarn/**",
    ".npn.*",
    "test"
  ]
}
