{
  "name": "@nfts/terminal",
  "version": "1.0.0",
  "scripts": {
    "test": "gmf build --test",
    "dev": "gmf build --watch",
    "build": "gmf build"
  },
  "devDependencies": {
    "@nfts/gmf": "workspace:*"
  }
}
