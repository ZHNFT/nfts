{
  "name": "@ntfs/interactive-query",
  "version": "0.0.0",
  "scripts": {
    "build": "npx tsc",
    "dev": "npx tsc --watch --declaration --declarationDir ./dist",
    "dts": "npx tsc --declaration --emitDeclarationOnly --declarationDir ./dist",
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "//devDependencies": {
    "@ntfs/eslint-config": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "@types/node": "~17.0.5",
    "jest": "^27.4.5",
    "ts-jest": "^27.1.2",
    "typescript": "^4.5.4"
  }
}
